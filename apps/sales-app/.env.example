# Mettre à true pour ignorer login/ProtectedRoute (dev sans backend auth)
VITE_AUTH_BYPASS=false

# URL de l'api-gateway (optionnel, défaut http://localhost)
# VITE_API_URL=http://localhost

# URL du portail (apps métier — redirect login si non authentifié)
VITE_PORTAL_URL=/
